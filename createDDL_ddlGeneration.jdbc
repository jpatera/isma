CREATE TABLE PERSON (ID BIGINT NOT NULL, CODE VARCHAR(255), NAME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE COMMENT (ID BIGINT NOT NULL, CREATED DATE, HEADER VARCHAR(255), TEXT VARCHAR(255), AUTHOR_ID BIGINT, PRIMARY KEY (ID))
ALTER TABLE COMMENT ADD CONSTRAINT COMMENT_AUTHOR_ID FOREIGN KEY (AUTHOR_ID) REFERENCES PERSON (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(15), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('COMMENT_TABLE_GEN', 0)
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('PERSON_TABLE_GEN', 0)
